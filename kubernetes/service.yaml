apiVersion: v1
kind: Service
metadata:
  name: microservice-producto
spec:
  selector:
    app: microservice-producto
  ports:
    - protocol: TCP
      port: 8089
      targetPort: 8089
  type: ClusterIP